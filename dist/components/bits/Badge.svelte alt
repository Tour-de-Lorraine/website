<script>
	import Icon from './Icon.svelte/index.js';
	const {icon, text = ''} = $props();
</script>

<button class="BADGE" data-text={text} data-as="ellipse" role="hidden" tabindex="0">
	<Icon name={icon}></Icon>
</button>

<style lang="scss">
	.BADGE {
		--ellipse-padding: 0em 0.4em;
		--icon-size: var(--icon-size-s);
		border: calc(var(--line-width) * 1.2) solid currentColor;
		position: relative;
		cursor: help;

		&::after {
			content: attr(data-text);
			position: absolute;
			max-width: 30ch;
			width: max-content;
			background-color: var(--color-theme-2);
			display: none;
			pointer-events: none;
			z-index: 9999999999;
			padding: var(--box-padding);
			font-size: var(--font-size-m);
			left: 0;
			top: 0;
			transform: translateY(-104%);
		}

		&:hover::after,
		&:focus::after {
			display: inline-block;
		}
	}

	/* Responsive
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~*/

	@media (width <= $bp-s-maxWidth) {
		.BADGE {
			position: unset;
		}

		.BADGE::after {
			max-width: 50vw;
			text-align: center;
			hyphens: auto;
		}
	}
</style>
